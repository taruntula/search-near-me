// $(document).ready(initApp);

// var searchTerm = $('input').val();
// var location = new Location();
// var yelp = new Yelp(references.zipcode, 10, searchTerm);
// var pokemon = new Pokemon(references.temp, DarkskyApi.weatherIcon);
// var weather = new DarkskyApi(references.longitude, references.latitude);

// var references = {
//   longitude: location.longitude,
//   latitude: location.latitude,
//   zipcode: location.zipCode,
//   temp: weather.currentWeather,
//   weather: weather.weatherIcon,
//   pokemon: pokemon.pokemonBasedOnTemp
// }

// console.log(location.latitude);
